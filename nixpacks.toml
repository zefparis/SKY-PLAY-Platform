[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm install --prefix services/api"]

[phases.build]
cmds = ["npm run build --prefix services/api"]

[start]
cmd = "cd services/api && node dist/main.js"
